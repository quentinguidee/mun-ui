import styled from '@emotion/styled'

export const MunApp = styled.div`
    font-family: 'Rubik', sans-serif;
    background-color: ${(props) => props.theme.color.background};
    min-height: 100vh;
`
